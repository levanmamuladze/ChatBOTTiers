# Copy to .env and drop in a Groq key to light up the chat.
# Without it the bots still load — they just answer with the polite fallback.
GROQ_API_KEY=gsk_your_key_here

# Optional. Defaults to openai/gpt-oss-20b if left out.
# GROQ_MODEL=openai/gpt-oss-20b
